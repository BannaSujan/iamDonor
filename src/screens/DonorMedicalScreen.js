import React from 'react';
import { StyleSheet, Text, View, Dimensions, StatusBar } from "react-native";
function DonorMedicalScreen({navigation}) {
    return (
        <View>
            <Text>Donor Medical Screen</Text>
        </View>
    );
}

export default DonorMedicalScreen;